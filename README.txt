For the database I used an in memory database from EntityFrameworkCore
The article model and the endpoints were implemented as requested

For frontend I used a simple Wpf app with a datagrid to display the the that
is retrived from the API. The datagrid si structured in pages of 10 articles each